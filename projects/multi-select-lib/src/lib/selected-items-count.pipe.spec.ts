import { SelectedItemsCountPipe } from './selected-items-count.pipe';

describe('SelectedItemsCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedItemsCountPipe();
    expect(pipe).toBeTruthy();
  });
});
